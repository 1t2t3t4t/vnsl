use crate::model::CompileOptions;

pub fn compile_json(options: CompileOptions) {
    println!("compile json {:#?}", options);
}

pub fn compile_csv(options: CompileOptions) {
    println!("compile csv {:#?}", options);
}
